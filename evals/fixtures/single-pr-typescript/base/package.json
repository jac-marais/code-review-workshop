{
  "name": "customer-summary-fixture",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "start": "node dist/src/server.js"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "playwright": "1.61.1",
    "typescript": "5.9.2"
  }
}
